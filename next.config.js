/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['tedsustentavel.com.br'],
    unoptimized: false,
  },
}

module.exports = nextConfig

